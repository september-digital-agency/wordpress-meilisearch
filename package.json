{
  "name": "wordpress-meilisearch-plugin",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run development",
    "development": "mix",
    "watch": "mix watch",
    "watch-poll": "mix watch -- --watch-options-poll=1000",
    "hot": "mix watch --hot",
    "prod": "npm run production",
    "production": "mix --production"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "devDependencies": {
    "laravel-mix": "^6.0.31"
  }
}
